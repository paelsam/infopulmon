import { AppRouter } from "./router/AppRouter";



export const App = () => {
  return (
    // This is the root component of the application
    // It should contain the global styles and the router
    // The router should contain the routes of the application
    <AppRouter />
  );
}